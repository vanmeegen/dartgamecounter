/**
 * GameConfigView - main view for game configuration (E2)
 * Placeholder - will be implemented in S21
 */

import type { JSX } from "react";
import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export function GameConfigView(): JSX.Element {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4">Game Setup</Typography>
      <Typography color="text.secondary">Coming in S21...</Typography>
    </Box>
  );
}
